const AUDIENCES = {
  PUBLIC: 'public',
  MEMBERS: 'members',
}

const ITEM_STATUSES = {
  PLANNED: 'planned',
  ACTIVE: 'active',
  COMPLETED: 'completed',
  CANCELLED: 'cancelled',
}

module.exports = {
  AUDIENCES,
  ITEM_STATUSES,
}
